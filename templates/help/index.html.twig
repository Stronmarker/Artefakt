{% extends 'base.logged.html.twig' %}

{% block title %}FAQ{% endblock %}

{% block body %}

<style>
    .faq-container {
        margin-top: 50px;
    }
    .faq-header {
        text-align: center;
    }
    .faq-header h1 {
        font-size: 3em;
        color: #097303;
    }
    .faq-section {
        margin-top: 50px;
    }
    .faq-item {
        background-color: #ffffff;
        margin-bottom: 20px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 10px;
    }
    .faq-question {
        font-size: 1.5em;
        color: #097303;
        margin-bottom: 10px;
        cursor: pointer;
    }

    .faq-answer {
        display: none;
        font-size: 1.2em;
        color: #000000;
        padding: 15px;
    }
    .arrow {
        margin-left: 10px;
        transition: transform 0.2s;
    }
    .arrow.down {
        transform: rotate(90deg);
    }
</style>

<div class="container faq-container">
    <div class="faq-header">
        <h1 class="ft_orbitron text-light">Foire Aux Questions</h1>
    </div>

    <div class="faq-section">
        <div class="faq-item">
            <div class="faq-question" onclick="toggleAnswer(this)">
                <h4>Comment fonctionne Artifact Vision ? <span class="arrow">&#9654;</span></h4>
            </div>
            <div class="faq-answer">
                <h5>Artifact Vision permet aux graphistes de partager leurs rendus 3D avec leurs clients pour obtenir des retours et des validations rapides.</h5>
            </div>
        </div>

        <div class="faq-item">
            <div class="faq-question" onclick="toggleAnswer(this)">
                <h4>Quels types de fichiers sont pris en charge ? <span class="arrow">&#9654;</span></h4>
            </div>
            <div class="faq-answer">
                <h5>Artifact Vision ne prends en charge que les fichiers .png pour les rendus 3D.</h5>
            </div>
        </div>

        <div class="faq-item">
            <div class="faq-question" onclick="toggleAnswer(this)">
                <h4>Comment puis-je inviter des clients à voir mes rendus ? <span class="arrow">&#9654;</span></h4>
            </div>
            <div class="faq-answer">
                <h5>Vous pouvez inviter des clients en partageant un lien direct depuis la plateforme Artifact Vision, où ils pourront voir et commenter vos rendus.</h5>
            </div>
        </div>
    </div>
</div>

<script>
    function toggleAnswer(element) {
        var answer = element.nextElementSibling;
        var arrow = element.querySelector('.arrow');
        if (answer.style.display === "none" || answer.style.display === "") {
            answer.style.display = "block";
            arrow.classList.add('down');
        } else {
            answer.style.display = "none";
            arrow.classList.remove('down');
        }
    }
</script>
{% endblock %}
